function startMockTest(testName) {
  if (testName === 'Mocktest1') {
    window.location.href = 'demo.html'; // Redirect to the specific HTML page
}
  // Add navigation or logic for starting the mock test here.
}
